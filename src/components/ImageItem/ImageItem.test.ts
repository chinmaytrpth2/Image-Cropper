import React from 'react';
import { shallow } from 'enzyme';
import ImageItem from './ImageItem';

describe('#Rendering the Image Component', () => {
    it('-Should render without any Error', () => {
        
    })
})